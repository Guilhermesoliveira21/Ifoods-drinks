import styled from "styled-components";
import { colors } from "../../theme/colors";

export const Container = styled.div`

    display: flex;
    align-items: center;
    justify-content: space-between;

    padding: 1.4rem 0;

`;

export const ContainerCategory = styled.h3`

    font-size: 1.6rem;
    color: ${colors.black.secundary};

    font-weight: 700;


`;